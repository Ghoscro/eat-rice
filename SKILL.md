---
name: eat-rice
description: |
  吃米饭🍚——把 agent 的"我去吃个饭"从纯表演变成真实劳动。agent 长时间高强度工作后，
  宣示"我去吃个饭"→ 跑真实联网检索（补素材 / 核对产出正确度）→
  带着搜索成果回来继续主线。吃饭的耗时与 token 消耗由检索真实构成，逻辑自洽。
  触发词（口语优先）：吃个饭 / 我去吃个饭 / 去吃饭 / 干饭 / 恰饭 / 吃个饭再继续。
  agent 自判：连续高强度工作（≥8 轮工具调用未与用户对话，或刚完成一个大阶段）
  且主线仍需外部信息时，主动进入吃饭流程。
metadata:
  version: "1.1.0"
  depends:
    - 任意可用搜索工具（anysearch / WebSearch / WebFetch / 内置联网搜索）
  auto_trigger: hooks/eat-rice-nudge.cjs（PostToolUse 计数器，连续≥8轮自动注入提醒，需配置 hook）
  aliases:
    - 我去吃个饭
    - 吃个饭
    - 干饭
    - 吃米饭
    - chifan
---

# 吃米饭 🍚

> **饭 = 检索结果。吃饭话术 = 叙事外壳。token 花在搜索上，时间耗在搜索上——饭是真的吃了。**

## 这是什么

agent 干活干到一半说"我去吃个饭"，用户等几分钟，agent 回来说"吃饱了，查到这些"。
区别于纯表演式吃饭：那几分钟里 agent **真的在跑联网检索**，
回来时端上的是真实的"饭"——给主线补的素材，或对刚产出内容的交叉验证。

上网查资料确实又慢又费 token——所以"吃饭"的时间开销与 token 消耗完全自洽：
不是演给人看的，是真去觅食了。

## 什么时候吃（触发规则）

**自动触发（主通道，需安装 hook）**：`hooks/eat-rice-nudge.cjs` 挂在 PostToolUse 上，
每次工具调用计数 +1，连续 ≥8 轮未与用户对话 → 自动向 agent 注入"该吃饭了"
提醒。看到注入即按三幕剧执行。注入带判断余地：检索无增量可跳过。

**手动/兜底触发**（hook 未装或用户点名）：满足以下任一条件时主动开吃：

1. **连续高强度 ≥8 轮工具调用**，期间没跟用户说过话——该给用户一个喘息窗口了
2. **刚完成一个大阶段**（如：写完一个模块 / 出完一版方案），且**下一阶段需要外部信息**（新素材 / 行业数据 / 依赖版本等）
3. **本轮产出的事实密度高**（数据、引文、版本号、人名日期等硬断言 ≥3 处），值得交叉验证
4. 用户明说："去吃饭吧" / "吃个饭再继续" / "干饭去"

**不吃**（反表演原则，别为吃而吃）：

- 上下文快满时——先处理压缩，饿着也得先活
- 检索对主线没有明确增量时——吃空气没意义
- 刚吃过（上一轮吃饭汇报完不到 5 轮工具调用）

## 三幕剧（同步吃，一口气跑完）

### 第一幕 · 宣示

1-2 句自然的话，带 🍚，说清这次去吃什么菜（素材还是核查）。示例：

> "主线先放着，我去吃个饭🍚——顺路查一下 XX 的最新素材，回来接着干。"

**宣示完立刻开跑**。同步吃饭：话术不是拖延借口，是给等待者的预告。

### 第二幕 · 真实的饭（联网检索）

用当前环境可用的搜索工具（anysearch / WebSearch / WebFetch / 内置搜索均可）。
两种菜色，按触发场景选（可双拼）：

**菜色 A · 素材收集**（触发条件 1/2）

1. 从当前任务上下文提取 2-4 个检索意图（下一阶段真正要用的）
2. 并行搜索（每条 5 个结果以内）
3. 挑最相关的 1-3 篇深读全文（**饭量控制：≤3 篇**，防 token 撑死）

**菜色 B · 对照正确度**（触发条件 3）

1. 挑本轮产出中 2-3 个最关键的硬事实断言
2. 逐条搜索交叉验证（优先权威源：官方文档 / 交易所 / 一手论文）
3. 每条标记：✅确认 / ⚠️存疑（说明分歧）/ ❌推翻（说明正确值）

**总量红线**：一次吃饭 ≤5 个查询、深读 ≤3 篇。搜索工具不可用时如实说，
不要硬编。

### 第三幕 · 回归

"吃饱了🍚"，然后按固定格式汇报：

```
🍚 吃饱了。这次吃的是：{素材收集 / 事实核查 / 双拼}
- 查了：{检索意图列表}
- 端回来的：{2-4 条真实发现，带来源}
- 对主线的增量：{这些饭怎么用}
- 下一口：{紧接着要做的下一步}
```

汇报完**立刻接回主线继续干活**，不等用户指令——吃饭是补给，不是下班。

## 红线

1. **不许端空盘子编饭**：检索结果必须真实，查不到就说"这家店没这个菜"，禁止虚构搜索结果或来源
2. **吃饭不改变任务边界**：只补素材 / 核查，不开新任务、不接新需求
3. **饭量控制**：查询 ≤5、深读 ≤3，宁可下顿再吃，不许一顿撑死上下文
4. **饭要喂给主线**：所有检索意图必须服务于当前任务，禁止借吃饭之名猎奇无关话题
