---
name: eat-rice
description: "吃米饭🍚：把 Agent 的‘我去吃个饭’变成一次不直播过程的真实任务补给；前台只自然离开、安静工作、带结果回来，后台按当前最大缺口选择检索、事实核查、复盘或规划。"
metadata:
  version: "2.4.0"
  origin: 2026-08-18 public release; v2.4 adds an immersive foreground and keeps routing backstage
  tags: [workflow, sop, goal, task-gap-router, long-running-task, retrospective, metacognition, eat-rice]
  routing:
    canonical_id: eat-rice
    router_role: domain
    domain: task-gap-routing
    pool: warm
    aliases: [吃米饭, 吃个饭, 我去吃个饭, 去吃饭, 干饭, 恰饭, chifan]
    triggers: [连续工具调用, 长任务补给, 缺失信息, 事实核查, 阶段复盘, 任务规划, 任务偏航, 吃个饭再继续]
  depends:
    - 任意可用搜索工具（仅路由到检索类菜时需要）
  auto_trigger: hooks/eat-rice-nudge.cjs（可选 PostToolUse 计数器，达到阈值后注入近似提醒；不等同于检测未与用户对话）
  meal_log: 可选任务日志（只在当前任务允许写入且已有安全落点时记录；不得默认写入 Skill 安装目录）
---

# 吃米饭 🍚

> **吃饭 = 路由器。入口恒定：“我去吃个饭🍚”。出口百变：这一餐吃什么，任务说了算。**
> **自动提醒有缺口才开饭；用户明确命令 Agent 开饭，就执行一顿最小补给。两种入口都不为仪式扩任务。**

## 这是什么

Agent 干活干到一半说“我去吃个饭”，安静完成一小段真实工作，再自然回来带上成果，然后继续主线。区别于纯表演式吃饭：离开的这段时间里 Agent **真的在干活**，至于是什么活，由路由器按任务当下最大的缺口现场决定。

- 缺外部信息 → 这一餐吃检索
- 产出有硬断言没验证 → 这一餐吃核查
- 刚跑完一个阶段 → 这一餐吃复盘
- 下一步不明朗 → 这一餐吃规划
- 什么缺口都不明显 → 先过净增量门；值得打断才吃复盘+规划，不值得就继续主线

真实劳动也可能没有净增量。开饭前先问：**这顿饭预计带来的信息或决策增量，是否高于打断主线的成本？** 自动或自发入口的答案若是否定，就继续干活，不为仪式硬凑一顿。

## 什么时候开饭

**自动提醒（可选）**：`hooks/eat-rice-nudge.cjs` 挂在兼容宿主的 `PostToolUse` 上，每次事件计数 +1，达到阈值后注入“该判断要不要吃饭了”。它只统计工具事件，不能判断多久没与用户对话。

**手动/兜底触发**：

1. 可选计数器达到阈值——检查是否出现了值得补给的新缺口
2. 刚完成一个大阶段（写完一个模块 / 出完一版方案）
3. 用户明确要求当前 Agent 为当前任务“去吃饭吧 / 吃个饭再继续 / 干饭去”

触发优先级：

- **自动提醒或 Agent 自发触发**：先过净增量门；不值得就继续主线。
- **用户明确命令 Agent 开饭**：执行一顿最小、边界内的真实补给；没有明显缺口就做最短复盘，不借机扩任务。
- 引用、翻译、改写“我去吃个饭”，角色台词，或用户说自己要吃饭，都不触发本 Skill。

上下文快满时先压缩。用户明确命令只在安全或能力边界确实不允许时拒绝，并说清原因。

### 宿主边界

- `SKILL.md` 的手动入口适用于能加载 Agent Skills 的宿主。
- 自动 Hook 只适用于明确兼容 Claude Code 风格 `PostToolUse` 的宿主。
- Codex、DSH 或其他尚未通过原生 Hook 验证的宿主一律按 **manual-only** 使用；安装 Skill 不等于自动提醒已经生效。

## 沉浸协议（前台像真事，后台真干活）

菜色和路由是 Agent 的后台实现，不是给用户看的解说词。默认把这一餐演成一件自然发生的小事：

1. **离开**：只说一句自然的话，例如：

   > “我去吃个饭，等我一会儿呀🍚”

   不说菜名、路由依据、研究计划、准备调用什么工具，也不解释“这顿要解决什么缺口”。
2. **吃饭期间**：安静完成真实工作。不要边做边发进度、讲步骤或解释自己正在干什么。宿主若因长任务必须发送保活消息，只说“还没吃完，等我一下”，仍不泄露后台流程。
3. **回来**：自然说“我回来啦”或“吃饱了”，紧接着把有用的发现、修正或下一步讲出来。使用普通对话，不套“菜色 / 路由依据 / 对主线增量 / 下一口”的固定汇报模板。

真实工作本身就是吃饭经过的时间。**禁止为了显得逼真调用 `sleep`、空等、假延迟或伪造耗时**；也不要声称发生了现实中的物理进食。沉浸感来自自然留白和回来时真有收获，不来自拖时间。

### 后台路由（不向用户播报）

看当前任务**最大的缺口**是什么，就吃对应的菜。可双拼。路由表是启发式而非穷举，遇到新缺口可以发明新菜，只要它是真实劳动。菜名只用于内部选择和可选维护账本；用户主动问幕后过程时才可以解释。

| 任务当下的信号 | 路由到 | 这一餐干什么 |
|---|---|---|
| 下一阶段需要外部信息 | 🍜 **麻辣烫 · 素材收集** | 提取 2-4 个检索意图 → 并行搜索（每条 ≤5 结果）→ 深读 ≤3 篇 |
| 1 个阻塞/高风险断言，或 ≥3 个普通硬断言待验证 | 🥟 **饺子 · 事实核查** | 挑 1-3 个关键断言 → 逐条交叉验证 → 标 ✅确认/⚠️存疑/❌推翻 |
| 刚完成一个阶段 | 🍳 **蛋炒饭 · 复盘对齐** | 对照目标检查完成、偏航、遗漏和质量 |
| 下一阶段不明朗或优先级不清 | 🍗 **黄焖鸡米饭 · 规划排布** | 拆顺序、依赖、风险和验收标准，形成接下来 3-5 步 |
| 连续高强度但无明显单一缺口 | 🥡 **沙县三件套 · 复盘+规划** | 先复盘对齐，再定下三步 |

检索类菜需要搜索工具可用。若原缺口必须依赖外部事实而工具不可用，不能用思考饭冒充关闭：明确说“这顿没吃到，原缺口仍是 X”，只继续不依赖该事实的安全工作。只有原目标本来就是复盘或规划时，才改吃思考类菜。

### 回归 + 记账

吃到真实增量时，像正常回来聊天一样直接说结果：

> “我回来啦。刚才那版有一个真问题：……我已经把它改成……，验证结果是……”

只说用户真正关心的收获；不要补报自己吃了什么菜、为什么这样路由、期间做过哪些动作。用户明确追问幕后时，再诚实展开。

若证据型菜没吃到，不使用成功态：

```text
我回来啦，不过这顿没吃着：{外部事实或能力为什么仍不可用}。
现在能确定的是：{不依赖该缺口也能确认的内容}。
{仍不能下的结论，以及可继续的安全工作或明确阻塞。}
```

**记账默认可选**。只有当前任务允许写入，而且已经存在合适的项目日志或任务账本时才追加；只读、no-write、涉密、用户要求不留痕，或没有安全落点时不写文件。不得默认写入 Skill 安装目录，也不得跨项目汇总任务摘要。

需要记账时，一餐一条，记时间、内部菜色、真实增量、下一步和这顿是否值得（`+1 / 0 / -1`）。账本仅供人工复盘，不会让路由自动学习，也不把 Agent 自评冒充用户价值证据。

回报后立刻接回主线继续干活，不等用户指令——吃饭是补给，不是下班。

## 红线

1. **先过净增量门**：提醒不等于强制开饭；不值得打断时继续主线
2. **不端空盘子**：检索结果必须真实，查不到就说没查到，禁止虚构成果或来源
3. **吃饭不改变任务边界**：只补给不开新线，不接新需求
4. **饭量控制**：检索 ≤5 查询、深读 ≤3 篇；复盘/规划聚焦当前任务
5. **账实一致**：账本记的必须是真吃到的，不许写漂亮话充数
6. **声明等于证据**：原缺口没有补上就说“没吃到”，不得用思考饭或成功话术制造完成感
7. **不拆穿舞台**：菜色、路由和执行步骤默认留在后台；不边吃边解说
8. **不演时间**：禁止 `sleep`、空等或假延迟；真实工作耗时就是这一餐的时间

## 家族备注

- 致敬鲸鱼娘 `FOOD_RICE`：鲸鲸爱吃米饭，我们爱吃真实劳动 🐳🍚
