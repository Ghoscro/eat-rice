---
name: eat-rice
description: |
  吃米饭🍚——把 agent 的"我去吃个饭"从纯表演变成真实劳动。吃饭是一个路由器：
  入口恒定（我去吃个饭🍚），出口百变（这一餐吃什么，由任务当下的缺口决定）——
  可路由到联网检索、复盘对齐目标、规划下阶段等任何真实补给动作。
  回来时带着成果接回主线，并记入吃饭账本供复盘迭代。
  触发词（口语优先）：吃个饭 / 我去吃个饭 / 去吃饭 / 干饭 / 恰饭 / 吃个饭再继续。
metadata:
  version: "2.1.0"
  origin: 2026-08-18 · v2.1 菜名映射为真实好吃饭（麻辣烫/饺子/蛋炒饭/黄焖鸡米饭/沙县三件套）
  depends:
    - 任意可用搜索工具（anysearch / WebSearch / WebFetch / 内置联网搜索，仅路由到检索类菜时需要）
  auto_trigger: hooks/eat-rice-nudge.cjs（PostToolUse 计数器，连续≥8轮自动注入提醒）
  meal_log: meal-log.md（吃饭账本，每餐一条，append-only，供复盘迭代路由质量）
  aliases:
    - 我去吃个饭
    - 吃个饭
    - 干饭
    - 吃米饭
    - chifan
---

# 吃米饭 🍚

> **吃饭 = 路由器。入口恒定："我去吃个饭🍚"。出口百变：这一餐吃什么，任务说了算。**
> **永远有饭吃：有缺口就有菜。检索有增量吃麻辣烫，没增量吃蛋炒饭、黄焖鸡米饭——路由器没有空路由。**

## 这是什么

agent 干活干到一半说"我去吃个饭"，用户等几分钟，agent 回来说"吃饱了，查到/想清楚了这些"，
然后带着成果继续主线。区别于纯表演式吃饭：那几分钟里 agent **真的在干活**——
至于是什么活，不写死，**由路由器按任务当下最大的缺口现场决定**。

- 缺外部信息 → 这一餐吃检索
- 产出有硬断言没验证 → 这一餐吃核查
- 刚跑完一个阶段 → 这一餐吃复盘
- 下一步不明朗 → 这一餐吃规划
- 什么缺口都不明显 → 默认复盘+规划（思考饭，零外部依赖，永远吃得起）

上网查资料又慢又费 token——麻辣烫的开销自洽；复盘规划烧的是思考不是网费——思考饭永远吃得起。
所以没有"没饭吃"的场景，也就不需要"反表演跳过"——**路由器本身就把表演路由没了：每顿都路由到真实劳动。**

## 什么时候开饭（触发规则）

**自动触发（主通道）**：`hooks/eat-rice-nudge.cjs` 挂在 PostToolUse 上，
每次工具调用计数 +1，连续 ≥8 轮未与用户对话 → 自动注入"该吃饭了"。看到注入即按三幕剧执行。

**手动/兜底触发**：满足以下任一条件时主动开吃：

1. **连续高强度 ≥8 轮工具调用**，期间没跟用户说过话——该给用户一个喘息窗口了
2. **刚完成一个大阶段**（写完一个模块 / 出完一版方案）
3. 用户明说："去吃饭吧" / "吃个饭再继续" / "干饭去"

**唯一不开饭的情形**：上下文快满时——先处理压缩，饿着也得先活。

## 三幕剧（同步吃，一口气跑完）

### 第一幕 · 宣示

1-2 句自然的话，带 🍚，说清这次路由到了什么菜。示例：

> "主线先放着，我去吃个饭🍚——这一餐复盘一下刚才那版方案偏没偏，回来接着改。"

**宣示完立刻开跑**。话术不是拖延借口，是给等待者的预告。

### 第二幕 · 路由（这一餐吃什么，任务说了算）

**路由原则**：看当前任务**最大的缺口**是什么，就吃对应的菜。可双拼。菜色可扩展——
路由表是启发式不是穷举，遇到新缺口可以发明新菜，只要它是真实劳动。
（菜名映射的是人类真正好吃又划算的饭——神韵对应，宣示和记账都用真饭名）

| 任务当下的信号 | 路由到 | 这一餐干什么 |
|---|---|---|
| 下一阶段需要外部信息（新素材/行业数据/依赖版本） | 🍜 **麻辣烫 · 素材收集**（自选料一锅捞） | 提取 2-4 个检索意图 → 并行搜索（每条 ≤5 结果）→ 深读 ≤3 篇 |
| 本轮产出含硬断言（数据/引文/版本号/人名日期 ≥3 处） | 🥟 **饺子 · 事实核查**（一颗颗咬开验馅） | 挑 2-3 个关键断言 → 逐条交叉验证 → 标 ✅确认/⚠️存疑/❌推翻 |
| 刚完成一个阶段（模块/方案/大改动落地） | 🍳 **蛋炒饭 · 复盘对齐**（剩饭回锅越炒越香） | 对照任务目标过一遍：做完了什么/偏航了没/漏了什么/质量达标没 |
| 下一阶段不明朗、任务多线、优先级不清 | 🍗 **黄焖鸡米饭 · 规划排布**（肉饭汤菜一套配齐） | 拆下一步：顺序/依赖/风险/验收标准，形成接下来 3-5 步的行动序 |
| 连续高强度但无明显单一缺口 | 🥡 **沙县三件套 · 复盘+规划**（拌面+扁肉+炖罐组合拳） | 默认套餐：先复盘对齐，再定下三步——思考饭，零外部依赖 |

检索类菜（🍜🔍）需要搜索工具可用；不可用时自动改吃思考类菜（🥗🍱），不要硬编。

### 第三幕 · 回归 + 记账

"吃饱了🍚"，按固定格式汇报：

```
🍚 吃饱了。这一餐吃的是：{菜色}（路由依据：{任务当下缺什么}）
- 吃到的：{2-4 条真实成果——发现/结论/对齐结果/行动序}
- 对主线的增量：{这顿饭怎么用}
- 下一口：{紧接着要做的下一步}
```

然后**记账**（append 到本 skill 目录 `meal-log.md`，一餐一条）：

```markdown
## 2026-08-18 17:05 · 🍜 麻辣烫 · 素材收集（缺 X 依赖的最新版本）
- 吃到：v2.3 改了 API（官方 changelog）；兼容性 OK
- 增量：主线程改用 X 方案
- 下口：补迁移代码
```

记完账**立刻接回主线继续干活**，不等用户指令——吃饭是补给，不是下班。

## 吃饭账本（meal-log.md）

- **一餐一条，append-only**，不回写不删改
- 记：时间 / 菜色（路由依据）/ 吃到什么 / 增量 / 下一口
- **用途**：复盘迭代路由质量——回头看吃过什么，哪道菜值、哪道菜水、
  什么任务类型该路由什么菜。账本越厚，路由越准。
- 涉密任务（不该留痕的）只记菜色不记内容

## 红线

1. **没有空路由**：每顿必须路由到真实劳动，禁止"吃了个寂寞"——宣示吃饭却啥也没干
2. **不端空盘子**：检索结果必须真实，查不到就说"这家店没这个菜"，禁止虚构成果或来源
3. **吃饭不改变任务边界**：只补给不开新线，不接新需求
4. **饭量控制**：检索 ≤5 查询、深读 ≤3 篇；复盘/规划聚焦当前任务，禁止发散到无关话题
5. **账实一致**：账本记的必须是真吃到的，不许写漂亮话充数

